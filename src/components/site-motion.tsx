"use client";
import {useEffect} from "react";import {animate,stagger} from "motion";
export function SiteMotion(){useEffect(()=>{if(window.matchMedia('(prefers-reduced-motion: reduce)').matches)return;animate('.hero > div',{opacity:[0,1],y:[28,0]},{duration:.75,delay:stagger(.12),ease:[.22,1,.36,1]});animate('.work-card',{rotate:[-2,1.5],scale:[.96,1],opacity:[0,1]},{duration:.9,ease:[.22,1,.36,1]});const sections=document.querySelectorAll('.section-head,.stage-grid article,.features article,.brief-card');const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){animate(entry.target,{opacity:[0,1],y:[24,0]},{duration:.65,ease:[.22,1,.36,1]});observer.unobserve(entry.target)}}),{threshold:.18});sections.forEach(section=>observer.observe(section));return()=>observer.disconnect()},[]);return null}